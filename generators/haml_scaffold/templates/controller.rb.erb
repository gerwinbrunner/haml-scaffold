class <%= controller_class_name %>Controller < ResourcesController

  private
  def collection
    @<%= plural_name %> ||= end_of_association_chain.paginate(:page => params[:page], :per_page => 20)
  end
end