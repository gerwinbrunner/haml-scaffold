class <%= controller_class_name %>Controller < ApplicationController
  inherit_resources

  private
  def collection
    @<%= plural_name %> ||= end_of_association_chain.paginate(:page => params[:page])
  end
end