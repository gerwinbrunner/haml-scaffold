- form_for(<%= singular_name %>) do |f|
  %fieldset
    = f.error_messages
<% for attribute in attributes -%>
    .field-2
      = f.label :<%= attribute.name %>
      .<%= attribute.field_type %>= f.<%= attribute.field_type %> :<%= attribute.name %>
<% end -%>
    .submit= f.submit "Submit"