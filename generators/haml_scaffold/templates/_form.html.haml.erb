#<%= plural_name %>_form
  - form_for(<%= singular_name %>) do |f|
    .form_block
      .error_block
        = f.error_messages
<% for attribute in attributes -%>
      .field
        = f.label :<%= attribute.name %>
      .input.<%= attribute.field_type %>
        = f.<%= attribute.field_type %> :<%= attribute.name %>
<% end -%>
      .submit
        = f.submit "Submit"