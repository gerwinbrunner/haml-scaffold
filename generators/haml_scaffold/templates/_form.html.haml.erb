#<%= plural_name %>_form
  - semantic_form_for(<%= singular_name %>) do |f|
    .error_block
      = f.error_messages
    - f.inputs do
<% for attribute in attributes -%>
      = f.input :<%= attribute.name %>
<% end -%>
    = f.buttons 
