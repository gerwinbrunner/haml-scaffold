- form_for @<%= singular_name %> do |f|
  = f.error_messages
  = details_table :object => @<%= singular_name %>, :only=>%w[<%= (attributes.map(&:name)-%w[id created_at updated_at]).join(" ") %>], :form => f
  %p= f.submit "Save <%= singular_name.humanize.downcase %>"